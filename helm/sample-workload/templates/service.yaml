apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-workload.fullname" . }}
  labels:
    {{- include "sample-workload.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample-workload.selectorLabels" . | nindent 4 }}
